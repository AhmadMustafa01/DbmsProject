$(document).ready(function() {
    // Initialize the datepicker for elements with the "datepicker" class
    $('.datepicker').datepicker({
        autoclose: true, // Automatically close the date picker when a date is selected
    });
});
